import styles from '../views.module.scss'

const TimelineView = ({
    text
}) => {
    return(
        <div className={styles.radio_button }>
            Timeline 
        </div>
    )
}

export default TimelineView;